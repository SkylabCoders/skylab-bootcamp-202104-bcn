/*const URL= '../../../src/common/utils/dataHeroes.json'*/

const searching = () =>{
  const search = document.querySelector('.input__searchbar');
  search.addEventListener('keyup', () => {
  });
}

window.onload = searching;
