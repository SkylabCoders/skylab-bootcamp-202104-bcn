const user = 'artaco89';
const password = 'Artaco89!';
const database = 'superHeroe';

export const uri = `mongodb+srv://${user}:${password}@cluster0.xoqla.mongodb.net/${database}?retryWrites=true&w=majority`;
