import React from 'react';
import heroes from '../assets/heroes';

function HeroesDetailComponent() {
  const heroe = heroes.find((element) => element.id === data.id));
  return (
    <ul>
    </ul>
  );
}

export default HeroesDetailComponent;