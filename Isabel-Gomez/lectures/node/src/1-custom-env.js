require('dotenv').config()

console.log(process); // en consola muestra todos los datos del process de Node

console.log(process.env.API_URL) // en consola muestra el valor de la APU_URL que hemos guardado en el .env