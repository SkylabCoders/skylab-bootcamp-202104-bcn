const ButtonUp = () => {
    // const [setCounter, ClickCounter] = useState(0);
    // return (<button onClick = {() => setCounter(ClickCounter + 1)}>+</button>)
    return <button onClick={() => { alert('vamos pal tren!'); }}>!</button>
}
export default ButtonUp;