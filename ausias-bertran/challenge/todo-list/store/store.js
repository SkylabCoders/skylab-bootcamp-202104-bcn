const state = {
    create:'create',
    read:'read',
    update:'update',
    delete:'delete'
};

const checkInputValue = 
    switch (inputValue) {
        case inputValue:
            state.newValue = inputValue;
            const listTodo = createTodoList();
            break;  
        default:
            alert('error');
            break;
    }

