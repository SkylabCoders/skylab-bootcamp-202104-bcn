/************TypeScript*************** */
function greeter(person) {
    return "Hello " + person;
}
var user = 'Jave User'; //especifico que la variable user sera de tipo String.
document.body.innerHTML = greeter(user);
